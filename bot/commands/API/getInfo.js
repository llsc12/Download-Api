const discord = require("discord.js")
module.exports = {
  name:'getinfo',
  aliases:['info', 'video', 'download', 'dl'],
  description:'Get information, links and downloads of a video',
  execute(client, message, args) {
    
  }
}